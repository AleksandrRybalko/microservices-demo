spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AleksandrRybalko/spring-cloud-config-server-repository
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: password